---
assignees:
- claude-code
position_column: todo
position_ordinal: '9180'
title: 'HotReloadLiveTests: gated live-model test fails with "cannot be completed into valid JSON"'
---
## What
The opt-in live-model test in `Tests/FoundationModelsSkillsTests/HotReloadLiveTests.swift` fails when it runs with `SKILLS_INTEGRATION_TESTS=1`. It records one issue at `HotReloadLiveTests.swift:66`: `Caught error: Encountered content that cannot be completed into valid JSON`. The failure is the same on a clean `main` tree and on the `^0drrr83` tree, so the cause is not in `SkillWatcher`. The default `swift test` run skips this test behind the env-var gate, so the suite reads green.

Found on 2026-08-26 while `^0drrr83` was in test.

## Acceptance Criteria
- [ ] `SKILLS_INTEGRATION_TESTS=1 swift test --filter HotReloadLiveTests` exits 0 on a machine where `SystemLanguageModel.default.isAvailable` is true
- [ ] The cause of the invalid-JSON output is found and recorded on this card (model output shape, prompt, or decoder)

## Tests
- [ ] `swift test` — exit 0