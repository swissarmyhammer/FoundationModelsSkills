---
comments:
- actor: claude-code
  id: 01kynrqm8q9n03zaq7yrjnyhxk
  text: 'Research done: read plan.md §4/§7.1/§9(#27,#29), read Extras'' FrontmatterDocument.split(text:) (returns (frontmatter: String?, body: String), textual-only), read Extras'' YAMLValue/LayeredYAMLDocument (used for the *stack''s* layered config merge, not skill frontmatter -- not reused here since it''s not Decodable and this task''s YAML decode is squarely ours per #29). Read all Examples/skill-library fixtures including the four broken/ fixtures and the existing test-local placeholder SkillFrontmatter in FixtureLibraryTests.swift (explicitly marked "the real decode type lands... in a later task" -- this task). Plan: SkillFrontmatter as a Decodable struct with a custom init(from:) that merges top-level + metadata.* extension fields (top-level wins, note recorded on conflict), plus a small local FrontmatterValue (Decodable YAML-value enum) for metadata''s arbitrary-typed values and for arguments'' dual string/list spelling. FrontmatterDecoder.decode(text:) -> Outcome{.decoded(DecodedSkill)/.skipped(reason:)}, with a line-targeted regex quoting-fallback retry on the description: line, matching the plan''s §4 "unquoted colon inside description" case. Proceeding with TDD per task workflow note.'
  timestamp: 2026-07-29T01:44:54.807981+00:00
- actor: claude-code
  id: 01kyns4wb1r0xwff1d1wj36xse
  text: 'TDD complete, GREEN: wrote Tests/FoundationModelsSkillsTests/FrontmatterDecoderTests.swift first (confirmed RED -- build failed, real SkillFrontmatter/FrontmatterDecoder types didn''t exist), then implemented Sources/FoundationModelsSkills/Frontmatter/SkillFrontmatter.swift (model: all spec fields + all extension fields, top-level-vs-metadata.* merge with top-level winning and a note recorded on conflict, unknown-top-level-key collection, a local FrontmatterValue YAML-value enum for metadata''s arbitrary values and arguments'' dual string/list spelling) and Sources/FoundationModelsSkills/Frontmatter/FrontmatterDecoder.swift (DecodedSkill + FrontmatterDecoder.decode(text:) -> Outcome{.decoded/.skipped}, with a line-targeted quoting-fallback retry on the description: line, never throwing). `swift build` and `swift test` both exit 0, 32/32 tests pass (21 new + 11 pre-existing). Also refactored Tests/FoundationModelsSkillsTests/FixtureLibraryTests.swift to drop its private placeholder SkillFrontmatter (explicitly documented as scaffolding for this task''s real type) and use the production types instead -- updated specCleanCarriesPureSpecFieldsWithExtensionsUnderMetadata''s assertions to match the new metadata-fallback resolution semantics (disableModelInvocation/preload now resolve to false via metadata.* rather than staying nil, since that fixture only sets them under metadata). Running really-done''s adversarial double-check now before handoff.'
  timestamp: 2026-07-29T01:52:09.057680+00:00
- actor: claude-code
  id: 01kynsqay1c32w265t3kytg719
  text: 'really-done complete. Adversarial double-check (round 1) returned REVISE with 4 findings: (1) the both-present-conflict-plus-note table only covered the 4 Bool extension fields, not arguments/argument-hint; (2) the retry-attempted-but-still-fails test''s assertion (!reason.isEmpty) didn''t actually distinguish the retry-fired path from the immediate-skip path; (3) the quoting-fallback retry''s single-physical-line limitation (multi-line folded description: values) was undocumented; (4) an unused `import Foundation` in SkillFrontmatter.swift. Fixed all four: added topLevelArgumentsWinsOverMetadataOnConflictAndRecordsANote + topLevelArgumentHintWinsOverMetadataOnConflictAndRecordsANote tests; strengthened the retry-failure test to assert reason.localizedCaseInsensitiveContains("retry"); added a "Known limitation" paragraph to quotingFallback''s doc comment; removed the unused import. Re-spawned double-check once (per the bounded-loop contract) -- it verified each fix against actual file content, additionally mutation-tested the two new tests by temporarily swapping topLevel/metadataValue for arguments/argument-hint in SkillFrontmatter.init(from:) and confirming both new tests genuinely fail (not vacuous), then reverted and reconfirmed green. Verdict: PASS. Final fresh verification (this session): `swift build` exit 0, `swift test` exit 0, 34/34 tests pass (23 FrontmatterDecoderTests + 10 FixtureLibraryTests + 1 PackageSmokeTests), zero warnings beyond pre-existing unrelated SwiftPM dependency-identity/mlx-bundle warnings. All acceptance criteria met. Leaving task in doing for /review per the implement skill''s process (does not move to review itself).'
  timestamp: 2026-07-29T02:02:13.825268+00:00
- actor: claude-code
  id: 01kyntq6qxkmpag1qhm1e04zgp
  text: |-
    Fixed all 8 open review findings:

    1-2. Added a `///` doc comment to `DecodedSkill.init(frontmatter:body:notes:)` in FrontmatterDecoder.swift explaining each parameter and how `notes` combines SkillFrontmatter's own conflict notes with the decoder's quoting-fallback-retry note.

    3. Fixed the doc-comment/behavior mismatch on `FrontmatterValue.spaceSeparatedOrListTokens` in SkillFrontmatter.swift: the old comment claimed both `allowed-tools:` and `arguments:` accept the dual space-separated-string/YAML-list spelling, but only `arguments:` actually does -- `allowedToolsRaw` decodes as a plain `String` and `allowedTools` tokenizes via simple `.split`, never through `FrontmatterValue`. Verified against plan.md §4 ("allowed-tools (a space-separated string, experimental in the spec)") and the existing passing tests (`allowedToolsIsKeptRawAndTokenized`, `allowedToolsIsEmptyWhenAbsent`) that already assert string-only behavior. Corrected the COMMENT to match the (intentionally correct) behavior rather than changing the behavior -- the task's own acceptance criteria only require dual-spelling for `arguments:`.

    4-7. Added `///` doc comments to SkillFrontmatter's memberwise `init(...)` and its `Decodable init(from decoder:)`, each explaining parameter semantics / decode strategy including the top-level-vs-metadata.* conflict resolution.

    8. Extracted the four near-identical Bool extension-field decodes (`preload`, `user-invocable`, `disable-model-invocation`, `partial`) in `init(from:)` into a local nested function `decodeBoolExtensionField(_ key: ExtensionKey) throws -> Bool?` (local rather than a private method so it can close over the local `container`), replacing the four duplicated decode call-sites with `try decodeBoolExtensionField(.caseName)`.

    Verification: `swift build` exit 0 (only pre-existing unrelated SwiftPM dependency-identity warnings). `swift test` exit 0, 34/34 tests pass, no new failures. Ran adversarial double-check via the really-done process before handoff.
  timestamp: 2026-07-29T02:19:38.109970+00:00
- actor: claude-code
  id: 01kynty6pqf3rw3qcqr9pbwrk4
  text: 'Adversarial double-check (really-done gate) returned PASS: confirmed all 8 findings genuinely addressed, the allowed-tools/arguments comment fix is correct (not a papered-over bug, cross-checked against plan.md §4/§5, the code, and existing tests), the decodeBoolExtensionField refactor covers all four boolean fields with no leftover duplication, the new doc comments are substantive, no scope creep, and a fresh `swift build`/`swift test` it ran itself both passed (34/34 tests). Checked off all 8 review-finding checklist items on the task description (progress now 1.0). Leaving task in `doing` per the implement skill''s process -- `/review` will pull it into `review` for the next gate.'
  timestamp: 2026-07-29T02:23:27.447889+00:00
depends_on:
- 01KYNCQ6ZFGBMZSHBY2W3EN080
position_column: doing
position_ordinal: '80'
title: Frontmatter model + Yams decode with quoting-fallback retry
---
## What
Decode skill frontmatter (plan §4, decision #27/#29). Extras' `FrontmatterDocument` does the textual split; YAML decoding is ours, with Yams.

- `Sources/FoundationModelsSkills/Frontmatter/SkillFrontmatter.swift` — model carrying every spec field: `name`, `description`, `license`, `compatibility`, `allowed-tools` (space-separated string, kept raw + tokenized), `metadata` (string-keyed map); and every extension field: `preload`, `user-invocable`, `disable-model-invocation`, `arguments` (space-separated string or YAML list), `argument-hint`, plus retired `partial`. Extension fields are accepted BOTH top-level (canonical) AND under `metadata.*`; top-level wins on conflict; record a diagnostic-worthy note when both are present. Unknown top-level keys are collected, never fatal.
- `Sources/FoundationModelsSkills/Frontmatter/FrontmatterDecoder.swift` — `FrontmatterDocument.split` → Yams decode. On a Yams parse error, run the quoting-fallback retry (plan §4): re-try with unquoted-colon `description:` values quoted; if the retry succeeds, decode + attach a diagnostic; if it fails, return a skip-with-diagnostic result, never throw out of the decoder.
- Output shape: `Result`-like `DecodedSkill` = (frontmatter, body, notes) or (skip reason) — the validator task consumes this.

## Acceptance Criteria
- [x] All spec + extension fields decode from top-level and from `metadata.*`
- [x] The `broken/` unquoted-colon fixture decodes via the retry with a diagnostic note
- [x] Truly unparseable YAML yields a skip result with a diagnostic, no throw
- [x] `arguments:` accepts both the space-separated string and YAML-list spellings

## Tests
- [x] `Tests/FoundationModelsSkillsTests/FrontmatterDecoderTests.swift` — table-driven: every field spelling; both `arguments` spellings; retry success; retry failure; unknown-keys collection
- [x] `swift test` — exit 0

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass.

## Review Findings (2026-07-28 21:05)

- [x] `Sources/FoundationModelsSkills/Frontmatter/FrontmatterDecoder.swift:24` — Public initializer lacks documentation comment — it is a public API entry point that needs to explain what values its parameters accept and what state it creates. Add a documentation comment above the init explaining the role of each parameter and the resulting struct state. Example: `/// Creates a decoded skill from frontmatter, body, and diagnostic notes.`.
- [x] `Sources/FoundationModelsSkills/Frontmatter/FrontmatterDecoder.swift:26` — Public memberwise initializer for DecodedSkill lacks a documentation comment; every public declaration must have a `///` doc comment. Add a documentation comment above line 26 explaining that this initializer constructs a DecodedSkill from its components (e.g., `/// Initializes a DecodedSkill with the decoded frontmatter, body, and accumulated notes.`).
- [x] `Sources/FoundationModelsSkills/Frontmatter/SkillFrontmatter.swift:62` — Comment claims `spaceSeparatedOrListTokens` is 'Shared by `allowed-tools:` and `arguments:`, both of which accept the same two spellings', but only `arguments` is implemented to support both spellings. The `allowed-tools` field is decoded as `String` and tokenized with simple `split`, so it accepts only string spelling, not array spelling. Either update the comment if `allowed-tools` intentionally supports string-only spelling, or change `allowedToolsRaw` to `FrontmatterValue?`, update the decoder to decode `allowed-tools` as `FrontmatterValue`, and update the `allowedTools` property to use `spaceSeparatedOrListTokens` — making both fields handle dual spellings consistently.
- [x] `Sources/FoundationModelsSkills/Frontmatter/SkillFrontmatter.swift:154` — Public initializer lacks documentation comment — this multi-parameter init is a primary public API for constructing `SkillFrontmatter` and needs to document its parameters and semantics. Add a documentation comment above the init explaining the purpose of the initializer and notable parameter semantics (e.g., nil defaults, optional vs. required fields, conflict resolution with metadata.*).
- [x] `Sources/FoundationModelsSkills/Frontmatter/SkillFrontmatter.swift:159` — Public Decodable conformance init(from:) lacks a documentation comment; every public declaration must have a `///` doc comment. Add a documentation comment above the init(from:) method explaining how SkillFrontmatter decodes from YAML, describing conflict resolution between top-level and metadata.* fields.
- [x] `Sources/FoundationModelsSkills/Frontmatter/SkillFrontmatter.swift:206` — Public memberwise initializer lacks a documentation comment; every public declaration must have a `///` doc comment per API Design Guidelines. Add a documentation comment above line 206 explaining the purpose and parameters of the memberwise initializer (e.g., `/// Initializes a SkillFrontmatter by directly assigning fields from YAML-decoded values.`).
- [x] `Sources/FoundationModelsSkills/Frontmatter/SkillFrontmatter.swift:228` — Public Decodable initializer lacks documentation comment — this is a custom decoder entry point that needs to explain its decoding strategy. Add a documentation comment explaining that this init decodes YAML frontmatter, resolves extension fields against metadata.*, and records conflict notes when both top-level and metadata.* values are present.
- [x] `Sources/FoundationModelsSkills/Frontmatter/SkillFrontmatter.swift:295` — Four extension boolean fields (topLevelPreload, topLevelUserInvocable, topLevelDisableModelInvocation, topLevelPartial) follow an identical decode pattern, differing only by the enum case name; extract a shared helper to reduce surface area. Extract a helper method: `private func decodeBoolExtensionField(_ key: ExtensionKey) throws -> Bool? { try container.decodeIfPresent(Bool.self, forKey: AnyCodingKey(stringValue: key.rawValue)) }` and replace the four decoding pairs with calls like `let topLevelPreload = try decodeBoolExtensionField(.preload)`.
