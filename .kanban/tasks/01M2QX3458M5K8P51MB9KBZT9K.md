---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m2qxdj2texmt28gkn0weckde
  text: |
    Research and implementation notes.

    **The shape of the data.** `RequestError.data` is `JSONValue?` (FoundationModelsACP `Core/JSONValue.swift`). `JSONValue` has no accessor for the object members, so a test must match the `.object` case to reach them.

    **The correction.** One private static helper `objectFields(in:)` in `CommandDispatchTests.swift` turns a `JSONValue?` into `[String: JSONValue]?`. Each of the two test bodies now reads:

    ```swift
    let fields = try #require(
        Self.objectFields(in: error.data),
        "expected object data, got \(String(describing: error.data))")
    ```

    A wrong shape now fails the test. The helper holds the only `guard`, and a helper `guard` gives a value to its caller; it does not end a test. The file already had this pattern in `commandNames(in:)`.

    **Proof that the new form can fail.** I made the production `RequestError.unknownCommand` build `data: .array([...])` instead of `.object([...])`, then ran the one test twice:

    - new `try #require` form: `Test run with 1 test in 1 suite failed ... with 1 issue` — `Expectation failed: Self.objectFields(in: error.data)` at CommandDispatchTests.swift:138:30, exit code 1.
    - `guard case .object(let fields) = try #require(error.data) else { return }`: `Test run with 1 test in 1 suite passed`, exit code 0.

    The production change was then reverted; `git status` shows only the test file modified.

    **Sweep of the whole ACPAgent test target.** 25 `guard` statements sit inside `@Test` function bodies. 14 of them have the same cause as this card — a bare `return` in the `else` branch, which ends the test. They are in SessionLifecycleTests (1), PromptTurnTests (4), CommandRegistryTests (2), InitializationTests (2), CancellationTests (1), and Integration/TierTwoTests (5). They are out of the scope of this card, so they are recorded as new card ^jv3nxd5. The rest are guards inside closures that give a value to the closure's caller, which is not this cause.
  timestamp: 2026-09-17T14:48:03.930812+00:00
- actor: claude-code
  id: 01m2qxedq29mrvh1w9h1xdj8s8
  text: |
    ### implement — changed
    - evidence: 1 file — /Users/wballard/github/swissarmyhammer/FoundationModelsACPAgent/Tests/FoundationModelsACPAgentTests/CommandDispatchTests.swift. Full `swift test` before: 563 tests in 61 suites, 16 issues (including 1 known issue), one failing suite, TierTwoTests with 15 issues. Full `swift test` after: the same — 563 tests in 61 suites, 16 issues (including 1 known issue), TierTwoTests the only failing suite. `swift test --filter CommandDispatchTests`: 6 tests in 1 suite passed. No new build warning; the one warning in the log ("missing creator for mutated node", the mlx-swift bundle) is in the before log too. Proof that the new form can fail: with the production data built as `.array` instead of `.object`, the new `try #require` form fails (1 issue, exit 1) and the old `guard ... else { return }` form passes (exit 0). The production change was reverted.
    - next: /review. The two untracked paths acp-agent.score_fix1.json and logs/ were not staged, committed, or deleted. Nothing is committed. Follow-up card ^jv3nxd5 holds the other 14 blocks with the same cause elsewhere in the test target.
  timestamp: 2026-09-17T14:48:32.226754+00:00
- actor: claude-code
  id: 01m2qxm38whw2yx029h2wj119s
  text: |
    ### review — clean

    By-hand review. The code is in the sibling repository `/Users/wballard/github/swissarmyhammer/FoundationModelsACPAgent` at commit `8548244`, which the `review` engine cannot reach.

    - evidence:
      1. **No test body can pass quietly.** Both test bodies now use `let fields = try #require(Self.objectFields(in: error.data), "expected object data, got ...")`. A wrong shape makes `#require` throw, the throw leaves the `catch` block, and the test fails. Two `guard` statements stay in the file, at `commandNames(in:)` and at `objectFields(in:)`. Both are private static helpers that give a value to a caller. Neither is in a test body. The card said one; there are two, and both are correct.
      2. **The proof runs.** `data: .object([...])` changed to `data: .array([...])` in `Sources/FoundationModelsACPAgent/Commands/CommandDispatch.swift`:
         - `unknownCommand` mutated → `swift test --filter anUnknownCommandRefusesWithANearMissAndNoModelTurn` FAILED, 1 issue at `CommandDispatchTests.swift:138:30`, message `Expectation failed: Self.objectFields(in: error.data)`.
         - `actionCommandAttachments` mutated → `swift test --filter anActionCommandWithAnAttachmentIsRefused` FAILED, 1 issue at `CommandDispatchTests.swift:219:30`, same message.
         - Both mutations reverted with `git checkout --`. The working tree holds no change.
      3. **The suite holds.** `swift test` → `563 tests in 61 suites failed after 1.821 seconds with 16 issues (including 1 known issue)`. The same 16 as before. All 15 open issues are in `TierTwoTests`, which card `^ry23gm` owns. `Suite CommandDispatchTests passed after 1.120 seconds`.
      4. **No new build warning.** `swift build --build-tests` gives one warning, `missing creator for mutated node: (.../mlx-swift_Cmlx.bundle/Contents/MacOS)`. That is a SwiftPM build-graph warning that was present before this commit. No source warning.

    - next: none. The card moves to `done`. The 14 other sites of the same cause are card `^jv3nxd5`, out of scope here.
  timestamp: 2026-09-17T14:51:38.140956+00:00
- actor: claude-code
  id: 01m2qxmtvy94nrzn3nejr8dzan
  text: |-
    ### finish iteration 1 — clean
    - implement: changed — Tests/FoundationModelsACPAgentTests/CommandDispatchTests.swift
    - test: the count holds — 563 tests in 61 suites, 16 issues before and after. TierTwoTests is the only failed suite (^fry23gm). CommandDispatchTests passes.
    - commit: 8548244 fix(tests): replace guard-return with require in two tests
    - review: clean — 0 findings
    - result: the card is in done.

    The proof, made two times: with the production data built as `.array` in place of `.object`, the new `try #require(Self.objectFields(in: error.data), ...)` form fails at CommandDispatchTests.swift:138 and :219; the old `guard case ... else { return }` form passed with exit 0. The production edit was reverted each time and the tree is clean.

    The two `guard` statements that stay in the file are in the private helpers `commandNames(in:)` and `objectFields(in:)`. Each gives a value to its caller and does not end a test, thus the rule is silent on them.

    14 more test bodies in other suites hold the same cause; they are ^jv3nxd5.
  timestamp: 2026-09-17T14:52:02.302103+00:00
position_column: done
position_ordinal: e980
title: 'ACPAgent: CommandDispatchTests uses guard case in two tests'
---
## What

**Cross-repository task.** The code is in `/Users/wballard/github/swissarmyhammer/FoundationModelsACPAgent`.

`Tests/FoundationModelsACPAgentTests/CommandDispatchTests.swift` holds two `guard case` blocks inside test bodies:

```swift
guard case .object(let fields) = try #require(error.data) else {
    ...
    return
}
```

The rule `code-hygiene/idioms-swift` `noGuardInTests` refuses a `guard` in a test. A `guard ... else { return }` makes the test end quietly and report a pass when the shape is wrong.

These two blocks are older than card `^321e315`. That card found them but did not touch them, because they are not the cause that card names.

Correct the cause in the whole file, not only the two lines.

- [x] Replace each `guard case ... else { return }` in a test body with a direct `#expect` or `try #require` that fails when the shape is wrong
- [x] Read the whole file for the same cause, and correct every instance

## Acceptance Criteria
- [x] No `guard` in a test body of `CommandDispatchTests.swift`
- [x] `swift test --filter CommandDispatchTests` passes
- [x] No new build warning

## Tests
- [x] `swift test --filter CommandDispatchTests`
